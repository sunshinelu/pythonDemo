# 需要使用 json 包解析 json 文件
import json
import numpy
import pandas


def analysis(file, user_id):
    times = 0
    minutes = 0

    # 完成剩余代码
    f = open(file)
    jsonFile = json.load(f)
    # if jsonFile['user_id'] == user_id:



    return times, minutes